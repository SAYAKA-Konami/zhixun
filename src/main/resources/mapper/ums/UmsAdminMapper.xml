<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.tiny.modules.ums.mapper.UmsAdminMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.macro.mall.tiny.modules.ums.model.UmsAdmin">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="icon" property="icon" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="note" property="note" />
        <result column="create_time" property="createTime" />
        <result column="login_time" property="loginTime" />
        <result column="status" property="status" />
        <result column="group_id" property="groupId" />
    </resultMap>


    <select id="getAdminIdList" resultType="java.lang.Long">
        SELECT
            DISTINCT ar.admin_id
        FROM
            ums_role_resource_relation rr
                LEFT JOIN ums_admin_role_relation ar ON rr.role_id = ar.role_id
        WHERE rr.resource_id=#{resourceId}
    </select>
    <resultMap id="UserVoMap" type="com.macro.mall.tiny.modules.ums.vo.UmsUserVo">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="icon" property="icon" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="note" property="note" />
        <result column="create_time" property="createTime" />
        <result column="status" property="status" />
        <result column="roleName" property="roleName" />
        <result column="groupName" property="groupName" />
    </resultMap>

    <select id="getAllUser" resultMap="UserVoMap">
        select a.id,a.username,a.icon,a.email,a.phone,a.note,a.create_time,a.status,
               r.name as roleName,
               g.name as groupName
        from ums_admin a
        left join ums_admin_role_relation rr on a.id = rr.admin_id
        left join ums_role r on rr.role_id = r.id
        left join `group` g on a.group_id = g.id
        <if test="username != null">
            where a.username like concat(#{username},'%')
        </if>
        limit #{pageSize} offset #{offsetSize}
    </select>

</mapper>
